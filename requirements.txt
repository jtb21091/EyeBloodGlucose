# ======================
# EyeBloodGlucose / Cherry ML Environment
# Tested Python 3.9–3.11 (macOS)
# ======================

# Video / vision
opencv-contrib-python==4.10.0.84

# Core scientific stack (NumPy 1.x line for MediaPipe compatibility)
numpy==1.26.4
scipy==1.11.4
pandas==2.2.3
matplotlib==3.9.2
seaborn==0.13.2
pillow==10.4.0

# ML libraries (latest compatible with NumPy 1.x)
scikit-learn==1.5.2
xgboost==2.1.3
lightgbm==4.6.0
catboost==1.2.8
statsmodels==0.14.2
joblib==1.5.1
tqdm==4.67.1
pyyaml==6.0.2
h5py==3.11.0

# MediaPipe + compatibility pins
mediapipe==0.10.21
protobuf==4.25.3

# TensorFlow/Keras (for any deep learning or feature extraction)
tensorflow==2.15.0
keras==2.15.0

# Apple / CoreML export (optional; can comment if builds fail)
coremltools==8.3

# ONNX conversion and runtime (for browser / serverless inference)
skl2onnx==1.17.0
onnx==1.17.0
onnxruntime==1.19.2

fastapi==0.95.2
uvicorn==0.22.0
python-multipart==0.0.6
